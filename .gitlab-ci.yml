image: node:latest

services:
  - redis:latest

cache:
  paths:
  - node_modules/

stages:
  - test
  - deploy

test:
  stage: test
  script:
    - npm install
    - npm start & sleep 1
    - npm test

deploy:
  stage: deploy
  script:
    - echo tbd
